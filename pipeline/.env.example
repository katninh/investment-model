# Pipeline env — copy to pipeline/.env and fill in. Never commit pipeline/.env.
# Mirror these to GitHub Actions repo secrets for scheduled runs.
DATABASE_URL=postgresql://postgres:PASSWORD@db.xxxx.supabase.co:5432/postgres
FRED_API_KEY=
TWELVEDATA_API_KEY=
# CoinGecko + pytrends need no key at low volume.
